<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" page.content="width=device-width, initial-scale=1.0">
  <title>{% if page.title %}{{ page.title }}{% else %}Kookboek{% endif %}</title>
  <style>
    :root {
      --primary-color: #001f3f;
      --secondary-color: #FFDC00;
      --background-color: #f7f7f7;
      --text-color: #333;
    }

    body {
      font-family: Verdana;
      background-color: var(--background-color);
      color: var(--text-color);
      line-height: 1.6;
      margin: 0;
      padding: 0;
    }

    header {
      background-color: var(--primary-color);
      color: white;
      text-align: center;
      padding: 2rem 0;
      margin-bottom: 2rem;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1rem;
    }

    .recipe-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }

    .recipe-card {
      background-color: white;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .recipe-image {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }

    .recipe-content {
      padding: 1.5rem;
    }

    .recipe-title {
      font-size: 1.4rem;
      margin-top: 0;
      color: var(--primary-color);
    }

    .recipe-title a {
      text-decoration: none;
      color: var(--primary-color);
    }

    .recipe-meta {
      display: flex;
      justify-content: space-between;
      margin: 1rem 0;
      font-size: 0.9rem;
      color: #777;
    }

    .recipe-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin-top: 1rem;
    }

    footer {
      background-color: var(--primary-color);
      color: white;
      text-align: center;
      padding: 1.5rem 0;
      margin-top: 3rem;
    }

    .recipe-detail {
      background-color: white;
      border-radius: 10px;
      padding: 2rem;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    /* Default color for other tags */
    .tag {
      background-color: var(--secondary-color);
      color: white;
      padding: 0.3rem 0.8rem;
      border-radius: 15px;
      font-size: 0.8rem;
    }

    /* Tag Colors */
    .tag-lunch { background-color: #FFB347; }
    .tag-gebak { background-color: #D2691E; }
    .tag-toetje { background-color: #8A5D9B; }
    .tag-makkelijk { background-color: #F4D03F; }
    .tag-prep { background-color: #9B59B6; }
    .tag-vegetarisch { background-color: #8ACB88; }
    .tag-veganistisch { background-color: #228B22; }
    .tag-vlees { background-color: #8B0000; }
    .tag-vis { background-color: #1E90FF; }
    </style>
</head>
<body>
  <header>
    <div class="container">
      <h1><a href="/kookboek" style="color: white; text-decoration: none;">Kookboek</a></h1>
      <p>Een collectie van recepten verzameld over de jaren heen</p>
    </div>
  </header>

  <main class="container">
    {{ page.content }}
  </main>
</body>
</html>
